"use client";

/**
 * @deprecated Per-page tours are now handled by the `usePageTour` hook
 * in each page component. This file is kept for reference only.
 */
export function FeatureTour() {
    return null;
}
